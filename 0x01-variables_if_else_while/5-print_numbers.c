#include <stdio.h>
/**
 *main - Entry point
 *Explain: 'from 0 to 9'
 *Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	return (0);
}
